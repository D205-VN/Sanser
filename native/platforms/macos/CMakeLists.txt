add_library(sanser-native-macos STATIC backend.mm metal_presenter.mm)
add_library(sanser-desktop-native ALIAS sanser-native-macos)
target_link_libraries(sanser-native-macos PUBLIC sanser-desktop-transport)
set_target_properties(sanser-native-macos PROPERTIES OBJCXX_STANDARD 20 OBJCXX_STANDARD_REQUIRED ON)
target_compile_options(sanser-native-macos PRIVATE -fobjc-arc -Wall -Wextra)
foreach(framework Cocoa VideoToolbox CoreMedia CoreVideo CoreImage ApplicationServices Metal MetalKit QuartzCore)
  find_library(${framework}_LIBRARY ${framework} REQUIRED)
  target_link_libraries(sanser-native-macos PUBLIC ${${framework}_LIBRARY})
endforeach()
target_link_libraries(sanser-native-macos PUBLIC "-weak_framework ScreenCaptureKit")
